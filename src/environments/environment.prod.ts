// Configuración de producción para Firebase + Render
export const environment = {
  production: true,
  apiUrl: 'https://tu-app-name.onrender.com/api/auth', // Cambiar por tu URL real de Render
  appName: 'Login System',
  version: '1.0.0',
  timeout: 5000,
  retryAttempts: 3
};
